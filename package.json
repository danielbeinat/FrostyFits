{
  "name": "frostyfits-fullstack",
  "version": "1.0.0",
  "description": "FrostyFits E-commerce - Full Stack Application",
  "workspaces": [
    "Frontend",
    "admin"
  ],
  "scripts": {
    "build": "npm run build --workspace=Frontend && npm run build --workspace=admin && rm -rf public && mv Frontend/dist public && mv admin/dist public/admin"
  },
  "dependencies": {
    "mongoose": "^8.5.0",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "joi": "^18.0.2",
    "cloudinary": "^1.21.0",
    "multer": "^1.4.5-lts.1",
    "multer-storage-cloudinary": "^4.0.0",
    "nodemailer": "^6.9.16"
  }
}